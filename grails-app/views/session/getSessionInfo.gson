import jsonview.Session

model {
    List<Session> sessionList = []
}
json tmpl.session(sessionList)
//json tmpl.sessionInfo(sessionList: sessionList)
//json g.render(template: 'sessionInfo', collection: "${sessionList}")